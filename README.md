# anti-hallucination-guard
Hallucination happens because generation and verification collapse into one step, Claude writes a confident-sounding sentence and never separately asks "do I actually know this, or does this need to be checked/flagged?
